{
  "name": "ProviderCredential",
  "description": "A ProviderCredential model",
  "base": "Base",
  "properties": {
    "id": {
      "type": "string",
      "id": true,
      "description": "'Provider credential ID'"
    },
    "userId": {
      "type": "string",
      "description": "'ID of owner user'",
      "required": "true"
    },
    "providerTypeId": {
      "type": "string",
      "description": "'ID of provider type'",
      "required": "true"
    },
    "credentials": {
      "type": "object",
      "description": "'Object with credentials key=>value pairs'",
      "required": "true"
    }
  }
}
